# Every dependency in package.json is pinned to an exact version, deliberately:
# a lockfile records what was installed, but a caret in the manifest still lets
# a fresh `npm install` resolve something the suite has never run against.
# Without this, the next `npm install <pkg>` quietly writes one back in.
save-exact=true
